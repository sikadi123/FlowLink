{
  "name": "flowlink",
  "version": "0.2.0",
  "description": "FlowLink Spring Boot + Netty + Vue 3 instant messaging demo.",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm.cmd run dev",
    "build": "cd frontend && npm.cmd run build",
    "backend": "cd backend && mvnw.cmd spring-boot:run \"-Dspring-boot.run.profiles=local\"",
    "start": "start-dev.cmd"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0"
}
